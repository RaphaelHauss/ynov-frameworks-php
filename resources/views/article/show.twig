{% extends 'layouts.app' %}

{% block content %}
	<div class="container">
		<h1>{{ article.title }} <a href="{{ route('article.edit', [article.id]) }}" class="btn btn-primary">Modifier l'article</a></h1>
		<p>Par {{ article.author.name }}</p>

		<p>{{ article.content }}</p>
	</div>
{% endblock %}
